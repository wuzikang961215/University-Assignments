public class LinearSearch extends MySearchAlg{

    // implement search method
    public int search(int [] array, int num){
        for(int i = 0; i < array.length; i++){
            if(array[i] == num)
                // return the index
                return i;
        }
        // when the num is not found
        return -1;
    }
}