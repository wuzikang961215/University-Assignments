class Shape{

    // attributes
    private double width;
    private double height;
    private double radius;
    private double length;

    // basic constructor
    public Shape(){
        this.width = 0;
        this.height = 0;
    }

    // parametric constructor
    public Shape(double width, double height, double radius, double length){
        this.width = width;
        this.height = height;
        this.radius = radius;
        this.length = length;
    }
    // for rectangle
    public Shape(double width, double length){
        this.width = width;
        this.length = length;
    }
    // for circle
    public Shape(double radius){
        this.radius = radius;
    }
    // for triangle
     public Shape(double height, double radius, double length){
        this.height = height;
        radius = 0;
        this.length = length;
    } 


    // accessors
    public double get_width(){
        return this.width;
    }
    public double get_height(){
        return this.height;
    }
    public double get_radius(){
        return this.radius;
    }
    public double get_length(){
        return this.length;
    }

    // mutators
    public void set_width(double width){
        this.width = width;
    }
    public void set_height(double height){
        this.height = height;
    }
    public void set_radius(double radius){
        this.radius = radius;
    }
    public void set_length(double length){
        this.length = length;
    }
}