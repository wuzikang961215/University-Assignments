//==================================
// Foundations of Computer Science
// Student: Zikang, Wu
// id: a1816094
// Semester: tr3
// Year: 2020
// Practical Exam Number: break room 1 
//===================================

public class DebuggingDemo {

    public void bugMethod() {
         int num[] = {1, 2, 3, 4};
         // the biggest of i in num[i] is 3 but not 5
         System.out.println(num[3]);
    // there expect to be a block in front of the "}" to finish the method
    }

    // the return type in the method is float rather than int
    public float bugMethod2() {
        // the float value must be 0.0f rather than 0.0
            float ans = 0.0f;
            System.out.println("This method had a bug!");
            return ans;
    // there expect to be a block in front of the "}"to finish the method
    }


}